<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.springtestdemo.mapper.thrusermapper.THrEmpUserMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="org.example.springtestdemo.entity.thruser.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="age" property="age" jdbcType="INTEGER"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="gender" property="gender" jdbcType="VARCHAR"/>
    </resultMap>


    <!-- 通用列 -->
    <sql id="Base_Column_List">
        select id, name, password, age, email, gender from sys_user
    </sql>

    <!-- 获取所有用户 -->
    <select id="listAllUsers" resultMap="BaseResultMap">
        <include refid="Base_Column_List"/>
    </select>

    <!-- 通过 id 获取用户 -->
    <select id="getUserById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        <include refid="Base_Column_List"/>
        WHERE id = #{id}
    </select>

    <!-- 查找同名用户（精确匹配） -->
    <select id="getUserByName" parameterType="java.lang.String" resultMap="BaseResultMap">
        <include refid="Base_Column_List"/>
        WHERE name = #{name}
    </select>

    <!-- 添加用户 -->
    <insert id="addUser" parameterType="org.example.springtestdemo.entity.thruser.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (
            name,
            password,
            age,
            email
        ) VALUES (
                     #{name},
                     #{password},
                     #{age},
                     #{email}
                 )
    </insert>

    <update id="updateUser" parameterType="org.example.springtestdemo.entity.thruser.User" useGeneratedKeys="true">
        UPDATE sys_user
        SET name = #{name},
            password = #{password},
            age = #{age},
            email = #{email}
        WHERE id = #{id}
    </update>
</mapper>
